{
  "name": "@cmm/snap",
  "version": "0.0.0",
  "private": true,
  "description": "CMM MetaMask Snap — Cardano + Midnight key derivation and signing",
  "license": "Apache-2.0",
  "main": "./dist/bundle.js",
  "files": ["dist", "images", "snap.manifest.json"],
  "scripts": {
    "build": "mm-snap build",
    "build:clean": "pnpm clean && pnpm build",
    "dev": "mm-snap watch",
    "serve": "mm-snap serve",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo '(snap: lint TBD with @metamask/eslint-config-snaps)'",
    "test": "echo '(snap: vitest to come in M1)'",
    "manifest": "mm-snap manifest"
  },
  "dependencies": {
    "@cmm/shared": "workspace:*",
    "@metamask/snaps-sdk": "^6.12.0"
  },
  "devDependencies": {
    "@metamask/snaps-cli": "^6.5.3",
    "@types/node": "^20.12.12"
  }
}
